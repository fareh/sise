{% extends '::base.html.twig' %}
{% block body %}

    <div class="rightMenu">

        {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'List', 'TitleContextValue' :  'ادارة بيانات المستخدمين', 'RouteAction' : '' })) }}

        <br>

        <div class="listLink">
            <ul>
                <li><a href="{{ path('securite_groupeutilisateur') }}">Groupes des utilisateurs</a></li>
                <li><a href="{{ path('securite_roles') }}">Listes des Roles</a></li>
                <li><a href="{{ path('sise_user_list') }}">Listes des utilisateurs</a></li>

            </ul>


        </div>

    </div>
<div class="leftMenu">

    {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'List', 'TitleContextValue' :  'المستخدمين', 'RouteAction' : 'fos_user_registration' })) }}

    <br>
    <div id="CPHMain_upGridView">
        <div>
            <table cellspacing="0" cellpadding="4" id="CPHMain_GV"
                   style="color:#333333;width:100%;border-collapse:collapse;">
                <tbody>
                <tr align="right" style="color:White;background-color:#507CD1;font-weight:bold;">
                    <th scope="col"><a href="#" style="color:White;">
                            الرمز</a></th>
                    <th scope="col"><a href="#" style="color:White;">الاسم
                            واللقب</a></th>
                    <th scope="col"><a href="#"
                                       style="color:White;">المستوى الإداري</a></th>
                    <th scope="col"><a href="#" style="color:White;">الهيكل
                            الاداري</a></th>
                    <th scope="col"><a href="#" style="color:White;">تاريخ
                            آخر تحديث</a></th>
                </tr>
                {% for user in users %}
                    <tr style="background-color:#EFF3FB;">
                        <td>
                            <a href="{{ path('fos_user_profile_edit') }}">{{ user.id|e }}</a>

                        </td>
                        <td>{{ user.nomprenutil|e }}</td>
                        <td>{{ user.codenivehier.libenivehierar|e }}</td>
                        <td>

                            {{ user.codeprof.libeprofar }}

                        </td>
                        <td>{{ user.lastLogin|date() }}</td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>


    </div>

    </div>


{% endblock %}

