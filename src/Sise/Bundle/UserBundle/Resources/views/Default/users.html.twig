{% extends '::base.html.twig' %}
{% block body %}
    {% trans_default_domain 'SiseCoreBundle' %}

    <div class="rightMenu">

        {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'List', 'TitleContextValue' :  'La gestion des données des utilisateurs'|trans(), 'RouteAction' : '' })) }}

        <br>

        <div class="listLink">
            <ul>
                <li><a href="{{ path('securite_groupeutilisateur') }}">{{'SecuriteGroupeutilisateur'|trans()  }}</a></li>
                <li><a href="{{ path('securite_roles') }}">{{'SecuriteProfil'|trans()  }}</a></li>
                <li><a href="{{ path('sise_user_list') }}">{{'SecuriteUtilisateur'|trans()  }}</a></li>

            </ul>


        </div>

    </div>
<div class="leftMenu">

    {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'List', 'TitleContextValue' :  'SecuriteUtilisateur'|trans(), 'RouteAction' : 'fos_user_registration' })) }}

    <br>
    <div id="CPHMain_upGridView">
        <div>
            <table cellspacing="0" cellpadding="4" id="CPHMain_GV"
                   style="color:#333333;width:100%;border-collapse:collapse;">
                <tbody>
                <tr align="right" style="color:White;background-color:#507CD1;font-weight:bold;">
                    <th scope="col"><a href="#" style="color:White;">{{ 'Codegrouutil'|trans() }}</a></th>
                    <th scope="col"><a href="#" style="color:White;">{{ 'Nomprenutil'|trans() }}</a></th>
                    <th scope="col"><a href="#"style="color:White;"> {{ 'User_Libenivehierar'|trans() }}</a></th>
                    <th scope="col"><a href="#" style="color:White;">{{ 'User_Libeprofar'|trans() }}</a></th>
                    <th scope="col"><a href="#" style="color:White;">{{ 'LastLogin'|trans() }}</a></th>
                </tr>
                {% for user in users %}
                    <tr style="background-color:#EFF3FB;">
                        <td>
                            <a href="{{ path('sise_user_edit',{'id': user.id|e}) }}">{{ user.id|e }}</a>

                        </td>
                        <td>{{ user.nomprenutil|e }}</td>
                        <td>{{ user.codenivehier.libenivehierar|e }}</td>
                        <td>

                            {{ user.codeprof.libeprofar }}

                        </td>
                        <td>{{ user.lastLogin|date() }}</td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>


    </div>

    </div>



{% endblock %}

