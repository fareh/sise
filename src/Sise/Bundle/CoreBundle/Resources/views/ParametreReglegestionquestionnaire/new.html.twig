{% extends '::base.html.twig' %}

{% block body -%}
    {% trans_default_domain 'SiseCoreBundle' %}
    {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'Edit', 'TitleContextValue' :  'ParametreReglegestionquestionnaire'|trans() , 'RouteAction' : 'securite_roles' })) }}
    <br>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate' }})}}
    <div id="securite">
        {{ form(form) }}

    </div>
    {{ form_end(form) }}
{% endblock %}
