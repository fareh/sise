{% extends '::base.html.twig' %}

{% block body -%}
     {{ render(controller("SiseCoreBundle:Default:menu" , { 'PageContext' : 'ConsQues', 'TitleContextValue' :'مدرسو التعليم العام (خاصّ بالمدارس الإبتدائية)', 'RouteAction' : 'effectiveenseignentlisteenseignenttypeenseignement' })) }}
    {{ include('SiseCoreBundle:Default:search.html.twig', {'form': search, 'pathfilter' : pathfilter}) }}
<br>
{% if entities is defined  and entities|length > 0 %}
    <table cellspacing="0" rules="all" id="CPHMain_GridView_Browse" style="border-color:#4F81BD;border-width:2px;border-style:Solid;width:100%;border-collapse:collapse;">
        <thead>
        <tr>
            <td align="center" colspan="4" style="color:White;background-color:#4F81BD;font-weight:bold;"></td>
        </tr><tr>
            <th align="center" scope="col" style="color:White;background-color:#4F81BD;">المعرف الوحيد</th>
            <th align="center" scope="col" style="color:White;background-color:#4F81BD;">الإسم و اللقب</th>
            <th align="center" scope="col" style="color:White;background-color:#4F81BD;">يدرسون الإنقليزية</th>
            <th align="center" scope="col" style="color:White;background-color:#4F81BD;">يدرسون تكنولوجيا المعلومات</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.idenuniqense }}</td>
                <td>{{ entity.nompren }}</td>
                <td>{{ entity.enseangl }}</td>
                <td>{{ entity.ensetechinfo }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endif %}
    {% endblock %}
