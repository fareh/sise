{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SecuriteGroupeutilisateur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Codegrouutil</th>
                <th>Libegrouutilfr</th>
                <th>Libegrouutilar</th>
                <th>Obse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('securite_groupeutilisateur_show', { 'id': entity.codegrouutil }) }}">{{ entity.codegrouutil }}</a></td>
                <td>{{ entity.libegrouutilfr }}</td>
                <td>{{ entity.libegrouutilar }}</td>
                <td>{{ entity.obse }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('securite_groupeutilisateur_show', { 'id': entity.codegrouutil }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('securite_groupeutilisateur_edit', { 'id': entity.codegrouutil }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('securite_groupeutilisateur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
